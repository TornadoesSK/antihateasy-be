# Backend for AntiHateApp

Very important not to hate! Spread love not hate!

## How to run

```
python main.py
```

To easily install these dependencies, you can run:

```
pip install -r requirements.txt
```